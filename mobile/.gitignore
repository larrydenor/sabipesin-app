node_modules/
.expo/
dist/
web-build/

# Local env — never commit real backend URLs / secrets
.env
.env.local

# macOS
.DS_Store

# Native build output (only present after `expo prebuild`)
/ios
/android
*.orig.*
